Name: Workflow_0d8b
SchemaVersion: "1.0"

# Optional - Set automatic triggers.
Triggers:
  - Type: Push
    Branches:
      - main

# Required - Define action configurations.
Actions:
  DeployAWSCloudFormationstack_22:
    Identifier: aws/cfn-deploy@v1.0.4
    Configuration:
      template: Testserv
      region: us-east-1
      name: test
    Environment:
      Connections:
        - Role: AWSRoleForCodeCatalystSupport
          Name: "314208970587"
      Name: dev
    Inputs:
      Sources:
        - WorkflowSource
